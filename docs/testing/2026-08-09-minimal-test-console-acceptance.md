# 最小化中控测试台验收记录

## 验收范围

本次只验收正式 Vue 前端从旧表单平台收敛为单页中控测试台：

```text
浏览器演示 → API Skill → 中控执行
```

后端、浏览器扩展、本地测试业务系统和真实 MES 执行连接均未修改。

## 页面结果

- 正式入口只挂载 `TestConsolePage`，不再包含侧边栏和旧页面导航。
- 页面保留自然语言任务输入、最新 Skill 学习结果和最近浏览器录制。
- 分析中的录制每 3 秒刷新；进入终态后停止轮询。
- 连接失败时保留上一次可见详情并显示明确错误。
- `api_candidate` 显示为“API Skill 已生成，等待业务系统配置执行连接”。
- 生产构建中已不存在“配置化表单执行智能体”“AI 生成配置”“外部业务系统”和
  “演示工作台”等旧界面文案。

## 自动验证

- 前端测试：8 个文件、16 个测试通过。
- TypeScript 检查和 Vite 生产构建通过。
- 应用标题为 `CommandCenter 测试台`。
- 桌面双列和窄屏单列规则已由组件结构与响应式样式覆盖。

Vite 仍提示 Element Plus 入口产物超过 500 kB，这是现有整包引入造成的构建警告，不影响
当前测试台功能；本次没有为了体积优化扩大改造范围。

## 现场数据

- 后端 `http://127.0.0.1:8000/docs`：HTTP 200。
- 前端 `http://127.0.0.1:5173/`：HTTP 200。
- 最新真实 MES 录制：`f72598a0-6766-4bd2-9bbb-4c15d39519fb`。
- 来源系统：`yifeng_mes`。
- 录制状态：`api_candidate`；分析阶段：`completed`。
- 候选 Skill：`查询采购申请列表`。
- 执行验证：`pending_system_connection`。

浏览器可视化控制在验收时因本机临时运行目录初始化失败而不可用，因此未生成自动化页面
截图；服务、接口数据、组件行为、响应式规则和生产构建已经完成验证。用户可直接打开前端
地址进行最终目视确认。

## 当前边界

自然语言执行区域会调用现有 `/task-runs` 接口，不在前端伪造执行成功。真实 MES Skill
仍需要后续安全执行连接和只读凭据；此前生成的 `api_candidate` 不会被前端当成可执行或已发布
Skill。
